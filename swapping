#include <iostream>
using namespace std;

void swap(int &, int &);

int main() {
    
    int x, y;
    x = 10;
    y = 20;
    swap(x,y);
	return 0;
}

void swap(int &m,int &n){
    int temp;
    temp = m;
    m=n;
    n=temp;
    cout<<m<<n;
}
